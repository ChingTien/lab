# For local development only; change the password immediately after first login.
ADMIN_INITIAL_PASSWORD=1234
PORT=8787
DB_PATH=./data/value-dialogue.db
# For the local Node API or an alternative Node host.
# CORS_ORIGIN=https://chingtien.github.io
# GitHub Pages front end: public Cloudflare Worker URL, without a trailing slash.
# VITE_API_BASE_URL=https://your-api.example.com
# This repository is published at /lab/.
# VITE_BASE_PATH=/lab/
